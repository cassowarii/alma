func main :
    let
        func message :
            print "The square of "
            print dup
            print " is ".
    in (
        (message | println square) 2
    ).

func square : * dup.
