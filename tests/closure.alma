func main:
    make-printer 4
    make-printer 5
    make-printer 6
    apply apply apply

    apply push-twice 3
    print print

    one-is-bigger 4
    println apply
    println apply
    .

func make-printer: → val [println val].

func push-twice val: [val val].

func one-is-bigger:
    dup
    → a [a]
    dip [ → a [ + 1 a ] ]
    .
