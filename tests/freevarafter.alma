# This is a pretty contrived example, but it's
# important to allow functions to be defined in
# any order, even if they have free variables

# If looking at github issue #5, this is a
# good test file

func main [ generate apply ]

func generate [
    2 → a (
        use
            func b [[f]]
            func f [a]
        in
            (b)
        end
    )
]
